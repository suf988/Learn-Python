# Q9: Can you change the values inside a list which is contained in set S?

s = {8, 7, 12, "Harry", [1,2]}

# this code will raise an error because sets can only contain immutable elements and list is mutable.
# so therefore, we can not change the values inside a list which is contained in a set.