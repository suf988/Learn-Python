# Q3: Can we have a set with 18 (int) and '18' (str) as a value in it?

# Yes we can have set with 18(int) and '18'(str) as a value.
# because both are different datatypes.

aSet = {33, 21, 18, 50, "18"}
print(f"Set: {aSet}")